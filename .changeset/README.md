# Changesets

Use `pnpm changeset` to add a release note for any package change.

Use `pnpm version-packages` to apply version bumps and changelog entries.

Use `pnpm release` to publish packages after versioning.
